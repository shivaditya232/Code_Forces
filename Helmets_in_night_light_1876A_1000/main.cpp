#include<iostream>
using namespace std;
int main(){
    int n;
    
    vector<pair<int,int>> v(n);
for (int i = 0; i < n; i++) cin >> v[i].first;
for (int i = 0; i < n; i++) cin >> v[i].second;
    return 0;
}